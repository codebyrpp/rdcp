export type Form = {
    id: string,
    projectId: string,
    name: string,
    description: string,
    createdAt: string,
    updatedAt: string,
    multipleResponses?: boolean,
    responseMessage?: string,
}